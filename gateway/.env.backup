# Database Configuration (similar to Dify main project)
# These variables are used to construct the DATABASE_URL dynamically
DB_HOST=db
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=difyai123456
DB_DATABASE=dify_gateway
DB_CHARSET=utf8
DB_EXTRAS=keepalives_idle=60&keepalives=1

# Application Configuration
PORT=3003
NODE_ENV=development

# Note: DATABASE_URL is now constructed dynamically in DatabaseConfigService
# Format: postgresql://username:password@host:port/database?extras

# docker模式
DATABASE_URL="postgresql://postgres:difyai123456@db:5432/dify_gateway?keepalives_idle=60&keepalives=1&client_encoding=utf8"
# debug模式
# DATABASE_URL="postgresql://postgres:difyai123456@localhost:5432/dify?keepalives_idle=60&keepalives=1&client_encoding=utf8"
